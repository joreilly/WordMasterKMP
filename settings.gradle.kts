pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}
rootProject.name = "WordMasterKMP"

include(":androidApp", ":shared", "compose-desktop")

